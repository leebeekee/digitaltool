# 📚 독서정보탐구 디지털 도구

초등학교 독서정보탐구 수업을 지원하기 위해 제작된 웹 기반 디지털 도구 모음입니다.  
학생들이 조별 활동 결과물을 공유하고, 서로의 작품을 공정하게 평가할 수 있도록 돕습니다.

## 🌟 주요 기능

### 1. 조별 사이트 등록 (`group_site.html`)
- 1조부터 11조까지 자신의 조를 선택하고 활동 결과물(URL)을 등록합니다.
- 조원들의 이름을 함께 기록하여 누가 참여했는지 알 수 있습니다.
- 등록된 사이트는 리스트 형태로 실시간 공유되어 다른 친구들이 쉽게 방문할 수 있습니다.
- **GitHub 연동**: 별도의 서버 없이 GitHub 저장소(`data.json`)에 데이터가 영구 보존됩니다.

### 2. 조별 작품 평가 (`evaluation.html`)
- 각 조의 작품을 보고 **창의성**, **완성도**, **디자인** 3가지 항목(5점 만점)으로 평가합니다.
- 평가 가중치 자동 계산:
  - 창의성 (50%)
  - 완성도 (40%)
  - 디자인 (10%)
- **관리자 모드**: 선생님은 관리자 비밀번호를 통해 투표 결과를 집계하고 순위를 확인할 수 있습니다.

## 🔧 설치 및 설정 (선생님용)

1. 이 저장소(Repository)를 자신의 GitHub 계정으로 복제(Fork)하거나 다운로드하여 업로드합니다.
2. `group_site.html`과 `evaluation.html` 파일을 열어 `githubConfig` 부분을 확인합니다. (이미 설정된 경우 생략)
   ```javascript
   let githubConfig = {
       owner: 'leebeekee',
       repo: 'digitaltool',
       // ...
   };
   ```
3. **GitHub Pages** 기능을 활성화하여 웹사이트로 배포합니다.
   - [Settings] -> [Pages] -> [Source]를 `main` 또는 `master` 브랜치로 설정.

## 🛠 기술 스택
- **HTML5 / CSS3 / JavaScript (Vanilla)**
- **Glassmorphism Design**: 최신 트렌드를 반영한 깔끔하고 세련된 UI
- **GitHub REST API**: Serverless 데이터 저장 구현

## ⚠️ 주의사항
- 이 프로젝트는 GitHub의 공개 저장소(Public Repo) 기능을 활용하여 데이터를 저장합니다.
- 학생들의 활동 데이터가 `data.json` 및 `eval_data.json` 파일에 누적됩니다.
- 수업 전후로 '데이터 초기화' 버튼(관리자용)을 사용하여 데이터를 정리할 수 있습니다.
